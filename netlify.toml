[build]
  functions = "netlify/functions"

[[headers]]
  for = "/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"

# Push notification cron - runs every hour, function checks if it's 8 or 20h Spain
[functions."push-send"]
  schedule = "0 6,18 * * *"
